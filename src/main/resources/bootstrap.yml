spring:
  cloud:
    sentinel:
      transport:
        dasshboard: https://sentinel.chenmx.net
    nacos:
      discovery:
        server-addr: https://nacos.chenmx.net
        namespace: dev
      config:
        server-addr: https://nacos.chenmx.net
        #文件后缀
        file-extension: yaml
        #指定分组
        group: dev
        #指定命名空间
        namespace: dev
  application:
    name: xxl-job-executor