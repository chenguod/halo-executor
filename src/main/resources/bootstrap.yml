spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://k8s.chenmx.net:32567
        namespace: dev
      config:
        server-addr: http://k8s.chenmx.net:32567
        #文件后缀
        file-extension: yaml
        #指定分组
        group: dev
        #指定命名空间
        namespace: dev
        enabled:
  application:
    name: xxl-job-executor