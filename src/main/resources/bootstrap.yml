spring:
  cloud:
    sentinel:
      transport:
        dasshboard: http://kubernetes.chenmx.net:32594
    nacos:
      discovery:
        server-addr: http://kubernetes.chenmx.net:32757
        namespace: dev
      config:
        server-addr: http://kubernetes.chenmx.net:32757
        #文件后缀
        file-extension: yaml
        #指定分组
        group: dev
        #指定命名空间
        namespace: dev
  application:
    name: xxl-job-executor